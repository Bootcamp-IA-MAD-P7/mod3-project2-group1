import { Check, ChevronDown, Globe } from "lucide-react"
import { useLanguage } from "@/app/providers/language-provider"
import { cn } from "@/lib/utils"
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel, DropdownMenuTrigger } from "@/shared/ui/dropdown-menu"
const options = [{ code: "en", key: "language.english" }, { code: "es", key: "language.spanish" }] as const
export function LanguageSelector() { const { language, setLanguage, t } = useLanguage(); return <DropdownMenu><DropdownMenuTrigger asChild><button type="button" aria-label={t("language.choose")} className="inline-flex h-9 items-center gap-2 rounded-lg border border-slate-200 bg-white px-2.5 text-sm font-medium text-slate-700 transition-colors hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-500 focus-visible:ring-offset-2 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200 dark:hover:bg-slate-700"><Globe className="size-4 text-slate-500 dark:text-slate-400" aria-hidden="true" /><span>{t(language === "en" ? "language.english" : "language.spanish")}</span><ChevronDown className="size-3.5 text-slate-400 dark:text-slate-500" aria-hidden="true" /></button></DropdownMenuTrigger><DropdownMenuContent align="end" className="min-w-40"><DropdownMenuLabel className="sr-only">{t("language.select")}</DropdownMenuLabel>{options.map((option) => { const active = option.code === language; return <DropdownMenuItem key={option.code} onSelect={() => setLanguage(option.code)} className={cn("cursor-pointer", active && "bg-violet-50 text-violet-700 dark:bg-violet-500/15 dark:text-violet-300")}><Check className={cn("size-4", active ? "opacity-100" : "opacity-0")} aria-hidden="true" />{t(option.key)}</DropdownMenuItem> })}</DropdownMenuContent></DropdownMenu> }
