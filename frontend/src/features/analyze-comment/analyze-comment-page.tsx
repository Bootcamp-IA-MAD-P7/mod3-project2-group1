import { MessageSquareText, ShieldCheck } from "lucide-react"
import { useState } from "react"
import { useLanguage } from "@/app/providers/language-provider"
import { AnalysisFeedback } from "@/features/analyze-comment/components/analysis-feedback"
import { CommentForm } from "@/features/analyze-comment/components/comment-form"
import { ManualAnalysisHttpError, httpManualAnalysisDataSource } from "@/features/analyze-comment/http-manual-analysis-data-source"
import type { ManualAnalysisDataSource } from "@/features/analyze-comment/manual-analysis-data-source"
import { validateComment, type AnalyzeCommentState, type ManualPrediction } from "@/features/analyze-comment/analyze-comment-view-model"
import { Badge } from "@/shared/ui/badge"
export function AnalyzeCommentPage({ dataSource = httpManualAnalysisDataSource }: { dataSource?: ManualAnalysisDataSource }) { const {t}=useLanguage(); const [comment,setComment]=useState(""); const [state,setState]=useState<AnalyzeCommentState>("initial"); const [prediction,setPrediction]=useState<ManualPrediction|null>(null); const validationError=validateComment(comment); const formError=state==="invalid"?(validationError?.startsWith("Enter a comment") ? t("comment.validation.required") : validationError ? t("comment.validation.maximum") : null):null; function handleCommentChange(value:string){setComment(value);setPrediction(null);setState(value?(validateComment(value)?"invalid":"ready"):"initial")} async function handleSubmit(){if(validateComment(comment)){setPrediction(null);setState("invalid");return}setPrediction(null);setState("submitting");try{setPrediction(await dataSource.createPrediction({text:comment}));setState("success")}catch(caught){setPrediction(null);setState(caught instanceof ManualAnalysisHttpError ? caught.apiError.status===422?"validation_error":caught.apiError.status===503?"backend_unavailable":"unexpected_error":"unexpected_error")}} return <main className="space-y-7 lg:space-y-8" aria-labelledby="analyze-comment-title"><section className="relative isolate overflow-hidden rounded-2xl border border-violet-200/80 bg-gradient-to-br from-violet-50 via-white to-violet-100/55 px-5 py-7 shadow-[0_16px_32px_rgba(109,40,147,0.1)] dark:border-violet-400/20 dark:from-violet-950/50 dark:via-slate-950 dark:to-violet-900/20 sm:px-7 sm:py-8"><div aria-hidden="true" className="pointer-events-none absolute -right-12 -top-16 size-52 rounded-full bg-violet-300/35 blur-3xl dark:bg-violet-400/10" /><div className="relative max-w-2xl"><Badge variant="outline" className="border-violet-300 bg-white/70 text-violet-800 dark:border-violet-400/30 dark:bg-violet-500/10 dark:text-violet-200"><MessageSquareText className="size-3" aria-hidden="true" /> CIVIKA</Badge><h1 id="analyze-comment-title" className="mt-5 text-3xl font-semibold tracking-tight text-slate-900 dark:text-white sm:text-4xl">{t("comment.title")}</h1><p className="mt-3 max-w-xl text-base leading-7 text-slate-600 dark:text-slate-300">{t("comment.intro")}</p><p className="mt-5 inline-flex items-center gap-2 rounded-full border border-violet-200/80 bg-white/60 px-3 py-1.5 text-xs font-medium text-violet-800 backdrop-blur-sm dark:border-violet-300/20 dark:bg-violet-950/25 dark:text-violet-100"><ShieldCheck className="size-3.5" aria-hidden="true" />{t("comment.formTitle")} <span aria-hidden="true">→</span> {t("common.hate")} / {t("common.nonHate")} <span aria-hidden="true">→</span> {t("comment.review")}</p></div></section><CommentForm value={comment} error={formError} isSubmitting={state==="submitting"} onChange={handleCommentChange} onSubmit={handleSubmit}/><AnalysisFeedback state={state} prediction={prediction}/></main> }
